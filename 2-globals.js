// ---Globals--(no window yani no dome  no query selector. you can access everything in file system)
// __dirname  = path to current directory
// __filename - file name
// //require - function to use modules (common js)
// modules - info abount current module(file)
// process - info about env(environment) where the program is executed
//  örnek :
//console.log(__dirname); //buraya yazdın  aşağı vscode terminale   node  2-globals.js(dosya ismi neyse o) yaz bunu gösteriyor. // it gives me the path.

// ________________
//ayrıca set interval ve settimeoutlar da var
// setInterval(() => {
//    console.log("hello");
// }, 1000);  durdurmak için control+c yapıyoruz

// yukarı ok  eski komutu getiriyor  vscode terminalde
//clear temizlemek
